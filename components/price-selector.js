import { getOffer } from "@/lib/offers";

export default function PriceSelector({ params }) {
        console.log(params.target.value)
        const game = selectedGame;

        return (console.log(params.target.value));
}